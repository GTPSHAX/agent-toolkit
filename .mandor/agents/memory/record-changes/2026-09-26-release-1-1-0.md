# 2026-09-26 — Release 1.1.0 to npm

## Scope
Minor version bump with the release tag and GitHub release notes. Branch
`chore/release-1.1.0` (base `main`), merged via PR #28.

## What changed
- `package.json`, `package-lock.json`: `1.0.1` → `1.1.0` via
  `npm version minor --no-git-tag-version`.
- Tag `v1.1.0` on `main`; GitHub release titled "New tools: file, time, csv,
  semver, path, and web upgrades" with notes covering the 20 → 30 tool growth
  (`web.request`, `regex`, `json.query`, `hash.file`, `base64.file`,
  `hex.file`, `time`, `csv`, `semver`, `path`) plus the `web.fetch` Markdown,
  crawl, and `format` upgrades.
- Publish ran through `.github/workflows/publish.yml` (tag push, OIDC trusted
  publishing, provenance). `npm view` confirms `dist-tags.latest = 1.1.0`.

## Verification
- `npm run build` exits 0 on the bump branch.
- Publish workflow run succeeded; npm registry shows `1.1.0` after the usual
  few-minute processing delay.
